
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <link href="@Url.Content("~/Layout.css")" rel="stylesheet" type="text/css" />
    <title></title>
</head>

<body>
    <nav>
        @if (IsSectionDefined("nav"))
        {
            @RenderSection("nav")
            <ul>
                <li><button class="bubbly-button" onclick="location.href = '@Url.Action("MainPage", "Home")'">My profile</button></li>
                <li><input type="button" class="bubbly-button" value="Make Order" onclick="location.href='@Url.Action("SeeBin", "Home")'" /></li>
                <li><input type="button" class="bubbly-button" value="Browse Restaurants" onclick="location.href='@Url.Action("BrowseRestaurants", "Home")'" /></li>
                <li><input type="button" class="bubbly-button" value="Exit" onclick="location.href='@Url.Action("Exit", "Home")'"></li>
            </ul>
            <ul float="right">
            </ul>
        }
        else
        {
        }

    </nav>
    @RenderBody()
</body>
</html>